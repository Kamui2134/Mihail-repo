export type CustomerType = {
	id: string
	last_reward_at: string
	last_stamp_at: string
	name: string
	phone: string
	stamps: number
}
export type CustomersDataType = {
	business_id: string
	sort: string
	limit: string
	offset: string
}
